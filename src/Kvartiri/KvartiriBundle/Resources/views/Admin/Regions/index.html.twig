{% extends "::layout/layoutAdmin.html.twig" %}

{% block body -%}
    <h1>Regions list</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('adminRegions_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                    <a href="{{ path('adminRegions_show', { 'id': entity.id }) }}">show</a>
                    <a href="{{ path('adminRegions_edit', { 'id': entity.id }) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('adminRegions_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
