{% extends "::layout/layoutAdmin.html.twig" %}

{% block body -%}
    <h1>Promotions list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('adminPromotions_show', { 'id': entity.id, 'hotelId': hotelId }) }}">{{ entity.id }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('adminPromotions_show', { 'id': entity.id, 'hotelId': hotelId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('adminPromotions_edit', { 'id': entity.id, 'hotelId': hotelId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('adminPromotions_new', { 'hotelId': hotelId }) }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
