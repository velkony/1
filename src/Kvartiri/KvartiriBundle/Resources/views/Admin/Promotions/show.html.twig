{% extends "::layout/layoutAdmin.html.twig" %}

{% block body -%}
    <h1>Promotions</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('adminPromotions', { 'hotelId': hotelId }) }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('adminPromotions_edit', { 'id': entity.id, 'hotelId': hotelId }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
