{% extends "::layout/layout.html.twig" %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1>{{ page[0].title }}</h1>
            <div class="span12">
                {{ page[0].contents|raw }}
            </div>
        </div>
    </div>
{% endblock %}
